pipeline {
    // instruct Jenkins to allocate an executor on any node
    agent any
    // set the pipeline stages
    stages {
        stage('Stage 1') {
            steps {
                echo 'Hello World!'
            }
        }
    }
}